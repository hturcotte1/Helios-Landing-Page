--- results.md(body)
+++ manuscript.md(body, ids/links undone)
@@ -15 +15 @@
-5. **(PROVED.) Partial results on Conjecture B.** The $d$-vector determines: $n$, the number of corners $r$, $f^\lambda$, the quantity $m=\min_i\min(a_i,b_i)$ of the corner-run parametrisation, $A_m+B_m$ (number of corner boxes with a side of length $m$), the product $\prod_i P_{a_i^{b_i}}(t)$ modulo $t^{2m+1}$ (the "local product theorem", Theorem 4.2), the sum of squared contents $C_2(\lambda)=\sum_{(i,j)\in\lambda}(j-i)^2$ (Theorem 4.6, via Jucys–Murphy elements), and further $C_1^2$ and $C_4$, while the sixth entry from the top is a function of the previous ones (Section 4.10, from a background agent's work that was re-verified here). Conversely, pairs of shapes with equal $f^\lambda$ and equal even content moments exist (first at $n=50$), so no proof can use the top of the vector through even content moments alone. Conjecture B is further proved for two-corner shapes with at least two unit parameters (Theorem 4.11.2), and — as within-class statements — for shapes with at most three rows and, for $n\ge21$, at most four rows (Theorems 4.12.2–4.12.3), via a new character-sum formula $n!P_\lambda(t)=\sum_{\tau\in S_n}\chi^\lambda(\tau^2)(1+t)^{\mathrm{fix}\,\tau}$ (Theorem 4.12.1). A proved infinite family of non-conjugate pairs agrees on a prefix of length $\approx n/3$ and on the top four entries simultaneously (Theorem 4.13.1), so a proof (or a counterexample) must control the middle of the vector. Consequently Conjecture B holds for all rectangles (Theorem 4.4) and — by a case analysis using explicit formulas — for all two-row shapes, two-column shapes and hooks, i.e. all shapes not containing $(3,2,1)$ (Theorem 4.9). A Pfaffian formula for $P_\lambda(t)$ (Theorem 4.7) gives an explicit formula for two-row shapes (Corollary 4.8). Additional results from the referee-checked background work are collected in Section 4.10.
+5. **(PROVED.) Partial results on Conjecture B.** The $d$-vector determines: $n$, the number of corners $r$, $f^\lambda$, the quantity $m=\min_i\min(a_i,b_i)$ of the corner-run parametrisation, $A_m+B_m$ (number of corner boxes with a side of length $m$), the product $\prod_i P_{a_i^{b_i}}(t)$ modulo $t^{2m+1}$ (the "local product theorem", Theorem 4.2), the sum of squared contents $C_2(\lambda)=\sum_{(i,j)\in\lambda}(j-i)^2$ (Theorem 4.6, via Jucys–Murphy elements), and further $C_1^2$ and $C_4$, while the sixth entry from the top is a function of the previous ones (Section 4.10; obtained in a separate computation and independently re-verified). Conversely, pairs of shapes with equal $f^\lambda$ and equal even content moments exist (first at $n=50$), so no proof can use the top of the vector through even content moments alone. Conjecture B is further proved for two-corner shapes with at least two unit parameters (Theorem 4.11.2), and — as within-class statements — for shapes with at most three rows and, for $n\ge21$, at most four rows (Theorems 4.12.2–4.12.3), via a new character-sum formula $n!P_\lambda(t)=\sum_{\tau\in S_n}\chi^\lambda(\tau^2)(1+t)^{\mathrm{fix}\,\tau}$ (Theorem 4.12.1). A proved infinite family of non-conjugate pairs agrees on a prefix of length $\approx n/3$ and on the top four entries simultaneously (Theorem 4.13.1), so a proof (or a counterexample) must control the middle of the vector. Consequently Conjecture B holds for all rectangles (Theorem 4.4) and — by a case analysis using explicit formulas — for all two-row shapes, two-column shapes and hooks, i.e. all shapes not containing $(3,2,1)$ (Theorem 4.9). A Pfaffian formula for $P_\lambda(t)$ (Theorem 4.7) gives an explicit formula for two-row shapes (Corollary 4.8). Additional results, obtained in separate computations and independently checked, are collected in Section 4.10.
@@ -44 +44 @@
-**The conjectures** (Worley, Problem 3, verbatim in `data/literature_search.md`):
+**The conjectures** (our formulation of the two halves of Worley's Problem 3):
@@ -310,5 +310,5 @@
-*Remarks.* (i) The proof only uses $d_1,d_2,d_3$, the entries $d_j$ for $j\le m+2$ and $j=\delta+1$, and $f^\lambda$; it is a case analysis rather than a structural argument, and does not obviously extend to general two-corner shapes $((a+c)^b,c^d)$, for which no closed form of the $d$-vector is available. (ii) An independent proof of the same theorem, produced by a background research agent and checked by two adversarial referees, is in `agent_notes/classC.md` (scripts `src/agents/classC_verify.py`, `classC_extra.py`); it proves Corollary 4.8 directly by a reflection-principle argument on words in $\{\text{row }1,\text{row }2\}$ and settles the last case by $f^{(\lambda_1,m)}=\binom{n-1}m-\binom{n-1}{m-2}<\binom{n-1}m=f^{(\lambda_1,1^m)}$ instead of the entry $d_{\delta+1}$. The two proofs were obtained independently and agree in every formula.
-
-### 4.10 What the top of the vector determines (top-end angle, re-verified)
-
-The background "top-end" agent (`agent_notes/topend.md`, scripts `src/agents/topend_*.py`) extended Theorem 4.6 systematically. Its proofs are elementary — explicit class expansions in $\mathbb Q[S_n]$ of power sums of the Jucys–Murphy elements, plus the fact that these act by contents — and everything below that is marked PROVED has a complete proof in that report which I read line by line; the formulas were **re-verified with my own independent code** in `src/check_topend.py` (Murnaghan–Nakayama characters for Theorem 4.10.1, the $d$-vector recursion for Theorems 4.10.2–4.10.3, hook lengths and contents for the separation data). Notation: for a partition $\rho$ without parts $1$, $K_\rho$ is the class sum of cycle type $\rho\cup1^{n-|\rho|}$ and $\omega_\lambda(K_\rho)=|K_\rho|\chi^\lambda(\rho\cup1^{n-|\rho|})/f^\lambda$ is its central character, $|K_\rho|=(n)_{|\rho|}/z_\rho$; $C_k=\sum_{\square}c(\square)^k$.
+*Remarks.* (i) The proof only uses $d_1,d_2,d_3$, the entries $d_j$ for $j\le m+2$ and $j=\delta+1$, and $f^\lambda$; it is a case analysis rather than a structural argument, and does not obviously extend to general two-corner shapes $((a+c)^b,c^d)$, for which no closed form of the $d$-vector is available. (ii) An independent proof of the same theorem was obtained separately and checked by two independent reviewers (scripts `src/agents/classC_verify.py`, `src/agents/classC_extra.py`); it proves Corollary 4.8 directly by a reflection-principle argument on words in $\{\text{row }1,\text{row }2\}$ and settles the last case by $f^{(\lambda_1,m)}=\binom{n-1}m-\binom{n-1}{m-2}<\binom{n-1}m=f^{(\lambda_1,1^m)}$ instead of the entry $d_{\delta+1}$. The two proofs were obtained independently and agree in every formula.
+
+### 4.10 What the top of the vector determines (re-verified)
+
+A separate computation (scripts `src/agents/topend_*.py`) extended Theorem 4.6 systematically. Its proofs are elementary — explicit class expansions in $\mathbb Q[S_n]$ of power sums of the Jucys–Murphy elements, plus the fact that these act by contents — and everything below that is marked PROVED has a complete proof that was checked line by line; the formulas were independently re-verified with separately written code in `src/check_topend.py` (Murnaghan–Nakayama characters for Theorem 4.10.1, the $d$-vector recursion for Theorems 4.10.2–4.10.3, hook lengths and contents for the separation data). Notation: for a partition $\rho$ without parts $1$, $K_\rho$ is the class sum of cycle type $\rho\cup1^{n-|\rho|}$ and $\omega_\lambda(K_\rho)=|K_\rho|\chi^\lambda(\rho\cup1^{n-|\rho|})/f^\lambda$ is its central character, $|K_\rho|=(n)_{|\rho|}/z_\rho$; $C_k=\sum_{\square}c(\square)^k$.
@@ -317 +317 @@
-$$\omega_\lambda(K_3)=C_2-\tbinom n2,\quad \omega_\lambda(K_{22})=\tfrac12C_1^2-\tfrac32C_2+\tbinom n2,\quad \omega_\lambda(K_5)=C_4-(3n-10)C_2-2C_1^2+\tfrac{n(n-1)(5n-19)}6,$$
+$$\begin{gathered}\omega_\lambda(K_3)=C_2-\tbinom n2,\qquad \omega_\lambda(K_{22})=\tfrac12C_1^2-\tfrac32C_2+\tbinom n2,\\ \omega_\lambda(K_5)=C_4-(3n-10)C_2-2C_1^2+\tfrac{n(n-1)(5n-19)}6,\end{gathered}$$
@@ -323,6 +323,6 @@
-$$\frac{(n)_6\,d_{n-6}(\lambda)}{f^\lambda}=(n^2-11n+42)C_1^2+2C_2^2+\Big(\tfrac{2n^3}3-16n^2+\tfrac{328n}3-210\Big)C_2+(n-15)C_4+\frac{n(n-1)(19n^4-326n^3+2489n^2-9856n+15390)}{180},$$
-so $d_{n-6}$ is a function of $(n,d_n,d_{n-3},d_{n-4},d_{n-5})$. (The polynomial as printed in the agent report omits the pure-$n$ term; I recovered it by interpolation and verified the corrected identity on all partitions of $6\le n\le22$, and the functional dependence on all partitions of $n\le30$.) *Proof.* Insert the $u_6$ table and $\omega_\lambda(K_{33})$. $\square$
-
-**Theorem 4.10.4 (polynomiality; PROVED in the report, proof read and found complete).** For every $\rho$ without parts $1$, $K_\rho(n)$ is a polynomial in $n$ and the power sums $\sum_kJ_k^m$ ($m\le|\rho|-\ell(\rho)$) of weighted degree $\le|\rho|-\ell(\rho)$ (weights $n\mapsto1$, $\sum J^m\mapsto m$), valid in $\mathbb Q[S_n]$ for all $n$. Consequently $\omega_\lambda(K_\rho)$ is a polynomial in $n,C_1,\dots,C_{|\rho|-\ell(\rho)}$ of that weighted degree, and each level polynomial $g_i=(n)_iu_i/f^\lambda$ is a polynomial in $n$ and content power sums, invariant under $C_k\mapsto(-1)^kC_k$. The report also determines (by full-rank certificates on $n\le16$ and exact solving, re-verified by the agent for $n\le20$; not independently recomputed here) the *new* invariant contributed by each level modulo the previous ones: level 7: $C_6-16C_1C_3$; level 8: $2C_3^2-(8n+164)C_1C_3$; level 9: $C_8-24C_1C_5-(32n-\tfrac{7720}3)C_1C_3$; level 10: $-24C_1C_2C_3+(12n^2+1060n+62664)C_1C_3-360C_1C_5$. Every new invariant beyond level 6 involves products of odd content moments.
-
-**Separation from the top (COMPUTATIONALLY VERIFIED).** $(n,f^\lambda,C_2,C_1^2,C_4)$ — i.e. $(n,d_n,d_{n-3},d_{n-4},d_{n-5})$ — separates all transpose classes of partitions of $n$ for every $n\le48$ and fails first at $n=49$, for $(9,8,8,6,3^5,1^3)$ vs $(11,7,5,5,4^4,3,1,1)$ (agent data $n\le58$; the $n\le49$ statement re-verified here). Adding $u_7$ separates all $n\le58$. With fewer levels: $(n,r,f,C_2)$ fails from $n=14$, $(n,r,f,C_2,C_1^2)$ from $n=28$. So the "top separating depth" is unbounded, as is the "bottom separating depth" (Section 4.6).
+$$\begin{gathered}\frac{(n)_6\,d_{n-6}(\lambda)}{f^\lambda}=(n^2-11n+42)C_1^2+2C_2^2+\Big(\tfrac{2n^3}3-16n^2+\tfrac{328n}3-210\Big)C_2\\ {}+(n-15)C_4+\frac{n(n-1)(19n^4-326n^3+2489n^2-9856n+15390)}{180},\end{gathered}$$
+so $d_{n-6}$ is a function of $(n,d_n,d_{n-3},d_{n-4},d_{n-5})$. (The polynomial as originally computed omitted the pure-$n$ term; it was recovered by interpolation, and the corrected identity was verified on all partitions of $6\le n\le22$, the functional dependence on all partitions of $n\le30$.) *Proof.* Insert the $u_6$ table and $\omega_\lambda(K_{33})$. $\square$
+
+**Theorem 4.10.4 (polynomiality; PROVED — proof obtained separately and checked line by line).** For every $\rho$ without parts $1$, $K_\rho(n)$ is a polynomial in $n$ and the power sums $\sum_kJ_k^m$ ($m\le|\rho|-\ell(\rho)$) of weighted degree $\le|\rho|-\ell(\rho)$ (weights $n\mapsto1$, $\sum J^m\mapsto m$), valid in $\mathbb Q[S_n]$ for all $n$. Consequently $\omega_\lambda(K_\rho)$ is a polynomial in $n,C_1,\dots,C_{|\rho|-\ell(\rho)}$ of that weighted degree, and each level polynomial $g_i=(n)_iu_i/f^\lambda$ is a polynomial in $n$ and content power sums, invariant under $C_k\mapsto(-1)^kC_k$. The same computation also determines (by full-rank certificates on $n\le16$ and exact solving, re-verified there for $n\le20$; not independently recomputed) the *new* invariant contributed by each level modulo the previous ones: level 7: $C_6-16C_1C_3$; level 8: $2C_3^2-(8n+164)C_1C_3$; level 9: $C_8-24C_1C_5-(32n-\tfrac{7720}3)C_1C_3$; level 10: $-24C_1C_2C_3+(12n^2+1060n+62664)C_1C_3-360C_1C_5$. Every new invariant beyond level 6 involves products of odd content moments.
+
+**Separation from the top (COMPUTATIONALLY VERIFIED).** $(n,f^\lambda,C_2,C_1^2,C_4)$ — i.e. $(n,d_n,d_{n-3},d_{n-4},d_{n-5})$ — separates all transpose classes of partitions of $n$ for every $n\le48$ and fails first at $n=49$, for $(9,8,8,6,3^5,1^3)$ vs $(11,7,5,5,4^4,3,1,1)$ (separate computation to $n\le58$; the $n\le49$ statement independently re-verified). Adding $u_7$ separates all $n\le58$. With fewer levels: $(n,r,f,C_2)$ fails from $n=14$, $(n,r,f,C_2,C_1^2)$ from $n=28$. So the "top separating depth" is unbounded, as is the "bottom separating depth" (Section 4.6).
@@ -332,9 +332,9 @@
-**Lemma U and the families $F_a$ (PROVED).** If the upper parts $\{\nu\subseteq\lambda:|\nu|\ge s\}$ and $\{\nu\subseteq\mu:|\nu|\ge s\}$ of the lower intervals of Young's lattice are isomorphic as graded posets, then $d_j(\lambda)=d_j(\mu)$ for $j\le n-s$. This explains the pairs $F_a=\{(a,1^{n-a}),(2^{a-1},1^{n-2a+2})\}$, $a\ge3$, which agree on $d_0,\dots,d_{n-2a+2}$; transposing, $F_a$ is the pair $\{(\lambda_1,1^m),(\lambda_1,m)\}$ of Proposition 4.6.1 with $m=a-1$. The agent's exhaustive check for $n\le40$ shows that for $n\ge8$ the pair $F_3$ is the *only* pair of transpose classes agreeing on $d_0,\dots,d_{n-4}$.
-
-**Referee verdicts.** Six independent adversarial referee agents (two each) checked Theorem 4.10.1 with its group-algebra identities, Theorem 4.10.4 with the rank-certificate argument, and the appendix results (closed forms for hooks and two-row shapes, sharpness of $F_a$, Kerov's transition formula), re-deriving every formula with their own code (identities in $\mathbb Z[S_n]$ for $n\le9$, Theorem 4.10.1 for all $\lambda\vdash n\le24$, etc.). All six verdicts were HOLDS. The only defect found is cosmetic: the proof of the $K_3^2$ expansion excludes the cycle type $(4,2)$ by calling it odd, which it is not; it is excluded because a product of two $3$-cycles moving six points has disjoint supports, hence type $(3,3)$.
-
-**Third pass of the top-end angle (appendix of `agent_notes/topend.md`; key data re-verified here).** (a) *Sharpness of $F_a$* (PROVED): the pair $F_a$ differs by exactly $1$ at $d_{n-2a+3}$ — the same statement as Proposition 4.6.1 after transposition, proved there by the two formulas and in the appendix again via Aitken's determinant $f^{\lambda/\nu}=|\lambda/\nu|!\det[1/(\lambda_i-\nu_j-i+j)!]$. (b) *Kerov's transition formula* (PROVED, elementary hook-length proof in the appendix; verified for all $\nu\vdash m\le26$): if $x$ is the content of an addable corner of $\nu\vdash m$, $X$ and $Y$ the content sets of the addable and removable corners, then $f^{\nu+x}/f^\nu=(m+1)\prod_{y\in Y}|x-y|/\prod_{x'\in X\setminus x}|x-x'|$; consequently a mirror box move $\lambda\mapsto\mu$ (content $-c\mapsto c$) preserves $f$ iff the two transition ratios of $\lambda'=\lambda-(\text{box }-c)$ towards contents $c$ and $-c$ coincide — an exact but Diophantine criterion. The tempting statement "$f^\mu=f^\lambda\iff C_1(\lambda)=-c$" is FALSE in both directions (first counterexamples at $n=20$ and $n=33$; VERIFIED). (c) *Proposition M0* (PROVED modulo the level polynomials of Theorem 4.10.4): two shapes with the same multiset of absolute contents, $C_1=0$ and equal $f$ agree on $d_{n-7},\dots,d_n$; if also $C_3^2$ agrees, on $d_{n-10},\dots,d_n$. (d) *Scan to $n=84$* (VERIFIED by the agent with a C++ sieve plus exact re-checks; the key pair re-verified here from hook lengths and contents): the number of pairs of transpose classes with equal $(n,f,C_2,C_1^2,C_4)$ is $0$ for $n\le48$ and grows to $76$ at $n=84$; the **first pair agreeing on the top eight entries $d_{n-7},\dots,d_n$ occurs at $n=84$**: $\lambda=(19,17,7,4^6,3,2^6,1,1)$, $\mu=(19,16,8,8,3^6,2^7,1)$, with equal $f$ (a 56-digit number), $C_1=0$, equal $C_{2k}$ for all $k$, $C_3=8298$ vs $1998$, $r=7$ vs $6$, and $d_{n-8}$ different (exact skew-tableau counts). So the "top separating depth" is $3$ for $n\le13$, $4$ for $n\le27$, $5$ for $n\le48$, $7$ for $n\le83$ and $\ge8$ from $n=84$ on. (e) The printed inversion formula for $C_2$ in the agent's Corollary B′ has a slip ($3f$, not $6f$, in the denominator); Theorem 4.6 above is the correct form.
-
-### 4.11 Two-corner shapes (fat-hook angle, re-verified)
-
-Write a two-corner shape as $\lambda=((a+c)^b,c^d)$ with corner runs $((a,b),(c,d))$, $a,b,c,d\ge1$, $n=ab+bc+cd$; transposition is $(a,b,c,d)\mapsto(d,c,b,a)$. The background "fat-hook" agent (`agent_notes/fathooks.md`) proved the following; its Lemma 1 was checked by two referees (HOLDS), and I re-verified the closed forms and Theorem 4.11.2 independently (`src/check_agents2.py`).
+**Lemma U and the families $F_a$ (PROVED).** If the upper parts $\{\nu\subseteq\lambda:|\nu|\ge s\}$ and $\{\nu\subseteq\mu:|\nu|\ge s\}$ of the lower intervals of Young's lattice are isomorphic as graded posets, then $d_j(\lambda)=d_j(\mu)$ for $j\le n-s$. This explains the pairs $F_a=\{(a,1^{n-a}),(2^{a-1},1^{n-2a+2})\}$, $a\ge3$, which agree on $d_0,\dots,d_{n-2a+2}$; transposing, $F_a$ is the pair $\{(\lambda_1,1^m),(\lambda_1,m)\}$ of Proposition 4.6.1 with $m=a-1$. An exhaustive check for $n\le40$ shows that for $n\ge8$ the pair $F_3$ is the *only* pair of transpose classes agreeing on $d_0,\dots,d_{n-4}$.
+
+**Independent review.** Six independent adversarial re-derivations (two each) checked Theorem 4.10.1 with its group-algebra identities, Theorem 4.10.4 with the rank-certificate argument, and further results (closed forms for hooks and two-row shapes, sharpness of $F_a$, Kerov's transition formula), re-deriving every formula with separately written code (identities in $\mathbb Z[S_n]$ for $n\le9$, Theorem 4.10.1 for all $\lambda\vdash n\le24$, etc.). All six confirmed the statements they checked. The only defect found is cosmetic: the proof of the $K_3^2$ expansion excludes the cycle type $(4,2)$ by calling it odd, which it is not; it is excluded because a product of two $3$-cycles moving six points has disjoint supports, hence type $(3,3)$.
+
+**Further results of the same computation (key data independently re-verified).** (a) *Sharpness of $F_a$* (PROVED): the pair $F_a$ differs by exactly $1$ at $d_{n-2a+3}$ — the same statement as Proposition 4.6.1 after transposition, proved there by the two formulas and again, separately, via Aitken's determinant $f^{\lambda/\nu}=|\lambda/\nu|!\det[1/(\lambda_i-\nu_j-i+j)!]$. (b) *Kerov's transition formula* (PROVED, elementary hook-length proof; verified for all $\nu\vdash m\le26$): if $x$ is the content of an addable corner of $\nu\vdash m$, $X$ and $Y$ the content sets of the addable and removable corners, then $f^{\nu+x}/f^\nu=(m+1)\prod_{y\in Y}|x-y|/\prod_{x'\in X\setminus x}|x-x'|$; consequently a mirror box move $\lambda\mapsto\mu$ (content $-c\mapsto c$) preserves $f$ iff the two transition ratios of $\lambda'=\lambda-(\text{box }-c)$ towards contents $c$ and $-c$ coincide — an exact but Diophantine criterion. The tempting statement "$f^\mu=f^\lambda\iff C_1(\lambda)=-c$" is FALSE in both directions (first counterexamples at $n=20$ and $n=33$; VERIFIED). (c) *Proposition M0* (PROVED modulo the level polynomials of Theorem 4.10.4): two shapes with the same multiset of absolute contents, $C_1=0$ and equal $f$ agree on $d_{n-7},\dots,d_n$; if also $C_3^2$ agrees, on $d_{n-10},\dots,d_n$. (d) *Scan to $n=84$* (VERIFIED with a C++ sieve plus exact re-checks; the key pair independently re-verified from hook lengths and contents): the number of pairs of transpose classes with equal $(n,f,C_2,C_1^2,C_4)$ is $0$ for $n\le48$ and grows to $76$ at $n=84$; the **first pair agreeing on the top eight entries $d_{n-7},\dots,d_n$ occurs at $n=84$**: $\lambda=(19,17,7,4^6,3,2^6,1,1)$, $\mu=(19,16,8,8,3^6,2^7,1)$, with equal $f$ (a 56-digit number), $C_1=0$, equal $C_{2k}$ for all $k$, $C_3=8298$ vs $1998$, $r=7$ vs $6$, and $d_{n-8}$ different (exact skew-tableau counts). So the "top separating depth" is $3$ for $n\le13$, $4$ for $n\le27$, $5$ for $n\le48$, $7$ for $n\le83$ and $\ge8$ from $n=84$ on.
+
+### 4.11 Two-corner shapes (re-verified)
+
+Write a two-corner shape as $\lambda=((a+c)^b,c^d)$ with corner runs $((a,b),(c,d))$, $a,b,c,d\ge1$, $n=ab+bc+cd$; transposition is $(a,b,c,d)\mapsto(d,c,b,a)$. The following was obtained in a separate computation; Lemma 4.11.1 was checked by two independent reviewers, and the closed forms and Theorem 4.11.2 were independently re-verified (`src/check_agents2.py`).
@@ -344 +344 @@
-**Theorem 4.11.2 (PROVED).** Conjecture B holds for every two-corner shape at least two of whose parameters equal $1$ — i.e. for the class $\mathcal C$ of Theorem 4.9 together with the families $F_1(c,d)=(c+1,c^d)$, $F_2(b,c)=((c+1)^b,c)$, $F_4=F_1^t$ with both parameters $\ge2$: if $\mu\vdash n$ and $d(\mu)=d(\lambda)$ then $\mu\in\{\lambda,\lambda^t\}$. *Proof sketch (full proof in the report).* $d_1,d_2$ force $\mu$ into $\mathcal C\cup F_1\cup F_2\cup F_4$ (Theorem 4.9, Step 2) and Theorem 4.9 excludes $\mu\in\mathcal C$; for the $F$-families one has closed forms: $d_j(F_2(b,c))=d_{j+1}((c+1)^{b+1})$ (removing the box $(b+1,c+1)$ first), and with $x$ the smaller parameter, $d_j=I_{j+1}$ for $j\le x$, $d_{x+1}=I_{x+2}-\varepsilon$ with $\varepsilon=[c=x]+[d=x]$ for $F_1$ and $1+[b=c]$ for $F_2$, plus explicit $d_{x+2}$; comparing $x$, $\varepsilon$, $n$ and $d_{x+2}$ settles all cases ($F_2$ vs $F_2$ reduces to rectangles, Theorem 4.4). All closed forms re-verified here for all such shapes with $n\le44$, and the theorem itself against all partitions of $n\le26$.
+**Theorem 4.11.2 (PROVED).** Conjecture B holds for every two-corner shape at least two of whose parameters equal $1$ — i.e. for the class $\mathcal C$ of Theorem 4.9 together with the families $F_1(c,d)=(c+1,c^d)$, $F_2(b,c)=((c+1)^b,c)$, $F_4=F_1^t$ with both parameters $\ge2$: if $\mu\vdash n$ and $d(\mu)=d(\lambda)$ then $\mu\in\{\lambda,\lambda^t\}$. *Proof sketch (the full case analysis is included with the code).* $d_1,d_2$ force $\mu$ into $\mathcal C\cup F_1\cup F_2\cup F_4$ (Theorem 4.9, Step 2) and Theorem 4.9 excludes $\mu\in\mathcal C$; for the $F$-families one has closed forms: $d_j(F_2(b,c))=d_{j+1}((c+1)^{b+1})$ (removing the box $(b+1,c+1)$ first), and with $x$ the smaller parameter, $d_j=I_{j+1}$ for $j\le x$, $d_{x+1}=I_{x+2}-\varepsilon$ with $\varepsilon=[c=x]+[d=x]$ for $F_1$ and $1+[b=c]$ for $F_2$, plus explicit $d_{x+2}$; comparing $x$, $\varepsilon$, $n$ and $d_{x+2}$ settles all cases ($F_2$ vs $F_2$ reduces to rectangles, Theorem 4.4). All closed forms re-verified here for all such shapes with $n\le44$, and the theorem itself against all partitions of $n\le26$.
@@ -348,3 +348,3 @@
-### 4.12 Structural results (structure angle, re-verified)
-
-The "structure" agent (`agent_notes/structure.md`) found the following; the character-sum formula is proved here directly from Lemma 4.5, and the two row-bounded theorems were checked line by line and their sign thresholds re-verified (`src/check_agents2.py`).
+### 4.12 Structural results (re-verified)
+
+The following results were obtained in a separate computation; the character-sum formula is proved here directly from Lemma 4.5, and the two row-bounded theorems were checked line by line and their sign thresholds independently re-verified (`src/check_agents2.py`).
@@ -358,3 +358,3 @@
-**Theorem 4.12.3 (at most four rows; PROVED).** If $\lambda,\mu\vdash n$ both have at most $4$ rows, $n\ge17$, and $d(\lambda)=d(\mu)$, then $\mu=\lambda$ (and $\mu\in\{\lambda,\lambda^t\}$ for all $n$, by Theorem 4.1 below $17$). *Proof.* With four coordinates $x_i=\lambda_i-i$: $n=p_1+10$, $C_1=\frac{p_2+p_1}2-10$, $C_2=\frac{2p_3+3p_2+p_1}6+20$, $C_4=\frac{6p_5+15p_4+10p_3-p_1}{30}+116$. The sign lemma gives $2C_1\ge\lambda_1(\lambda_1-1)-20$, so $C_1>0$ for $n\ge21$; an exhaustive check of the $\le4$-row shapes with $n\le20$ shows that the largest one with $C_1\le0$ is $(4,4,4,4)$, $n=16$, so $C_1>0$ for all $n\ge17$ (the agent stated $n\ge21$; its referees and my check give $17$). Thus $p_1,p_2,p_3$ and $e_1,e_2,e_3$ are determined; Newton's identities $p_4=e_1p_3-e_2p_2+e_3p_1-4e_4$ and $p_5=e_1p_4-e_2p_3+e_3p_2-e_4p_1$ make $30(C_4-116)$ an affine function of $e_4$ with slope $-30(e_1+2)=-30(n-8)\ne0$; so $e_4$, the quartic $\prod(T-x_i)$, and $\lambda$ are determined. $\square$ *Remarks.* Both theorems are within-class statements (both shapes row-bounded); five rows would need one more even-degree invariant independent of $C_1^2,C_2,C_4$ (the level-7 invariant $C_6-16C_1C_3$ involves an odd moment).
-
-**Theorem 4.12.4 (Pfaffian expansion; PROVED by the agent, three-row corollary re-verified here for all three-row shapes with $n\le18$).** Expanding the Pfaffian of Theorem 4.7 along its last index (with $\ell>\ell(\lambda)$ even, so the last entries are truncated exponentials $E_k(t)=\sum_{j\le k}t^j/j!$) gives $P_\lambda=\sum_{j<\ell}(-1)^{j+1}E_{\alpha_j-1}\,\mathrm{Pf}(B^{(\hat j\hat\ell)})$; for three rows,
+**Theorem 4.12.3 (at most four rows; PROVED).** If $\lambda,\mu\vdash n$ both have at most $4$ rows, $n\ge17$, and $d(\lambda)=d(\mu)$, then $\mu=\lambda$ (and $\mu\in\{\lambda,\lambda^t\}$ for all $n$, by Theorem 4.1 below $17$). *Proof.* With four coordinates $x_i=\lambda_i-i$: $n=p_1+10$, $C_1=\frac{p_2+p_1}2-10$, $C_2=\frac{2p_3+3p_2+p_1}6+20$, $C_4=\frac{6p_5+15p_4+10p_3-p_1}{30}+116$. The sign lemma gives $2C_1\ge\lambda_1(\lambda_1-1)-20$, so $C_1>0$ for $n\ge21$; an exhaustive check of the $\le4$-row shapes with $n\le20$ shows that the largest one with $C_1\le0$ is $(4,4,4,4)$, $n=16$, so $C_1>0$ for all $n\ge17$ (the original derivation stated $n\ge21$; the exhaustive check gives $17$). Thus $p_1,p_2,p_3$ and $e_1,e_2,e_3$ are determined; Newton's identities $p_4=e_1p_3-e_2p_2+e_3p_1-4e_4$ and $p_5=e_1p_4-e_2p_3+e_3p_2-e_4p_1$ make $30(C_4-116)$ an affine function of $e_4$ with slope $-30(e_1+2)=-30(n-8)\ne0$; so $e_4$, the quartic $\prod(T-x_i)$, and $\lambda$ are determined. $\square$ *Remarks.* Both theorems are within-class statements (both shapes row-bounded); five rows would need one more even-degree invariant independent of $C_1^2,C_2,C_4$ (the level-7 invariant $C_6-16C_1C_3$ involves an odd moment).
+
+**Theorem 4.12.4 (Pfaffian expansion; PROVED separately, three-row corollary independently re-verified for all three-row shapes with $n\le18$).** Expanding the Pfaffian of Theorem 4.7 along its last index (with $\ell>\ell(\lambda)$ even, so the last entries are truncated exponentials $E_k(t)=\sum_{j\le k}t^j/j!$) gives $P_\lambda=\sum_{j<\ell}(-1)^{j+1}E_{\alpha_j-1}\,\mathrm{Pf}(B^{(\hat j\hat\ell)})$; for three rows,
@@ -362,11 +362,11 @@
-with the two-row polynomials of Corollary 4.8. (A negative result from the same angle, VERIFIED for $n\le10$: there is no constant-coefficient bilinear differential identity expressing $\sum_cP_{\lambda-c}(t)P_{\lambda-c}(u)$ through $P_\lambda$ and its derivatives, so the multiset of children polynomials cannot be read off $P_\lambda$ by such a formula.)
-
-### 4.13 The skeptic angle: independent confirmation, a both-ends family, and a heuristic
-
-A "skeptic" agent (`agent_notes/skeptic.md`, `skeptic2.md`; referees HOLDS on its proved claims) tried to disprove Conjecture B.
-
-**Independent confirmation (COMPUTATIONALLY VERIFIED).** Its from-scratch $d$-vector code reproduces, for every $n\le42$, the counts, the number of transpose pairs, the absence of collisions and the digests of `data/collisions_python.txt`. A targeted search for pairs of transpose classes with the same multiset of corner boxes, the same $f^\lambda$ and the same $(C_2,C_1^2,C_4)$ finds none for $n\le52$.
-
-**Theorem 4.13.1 (a family agreeing at both ends; PROVED by the agent, re-verified here for $k\le7$).** For $k\ge1$ let $\lambda_k=(5,3^k,2,2,2)$ and $\mu_k=(4,4,3^k,1,1,1)$, $n=3k+11$. Then $f^{\lambda_k}=f^{\mu_k}$, $C_2(\lambda_k)=C_2(\mu_k)$, $C_1(\lambda_k)-C_1(\mu_k)=2$, and $d_j(\lambda_k)=d_j(\mu_k)$ for $0\le j\le k$ and for $n-3\le j\le n$, while $d_{n-4}$ differs. So the two shapes agree on a prefix of length about $n/3$ *and* on the top four entries (the first place where $C_1^2$ enters, $d_{n-4}$, separates them). The bottom agreement rests on a "body insertion" lemma reducing it to $d_j((3^K,2,2,2))=d_j((3^K,1,1,1))$ for $j\le K$, which after transposition and a $180^\circ$ rotation becomes a symmetry $N_j(\nu)=N_j(\nu^*)$ of chain counts in the three-row part of Young's lattice, $\nu^*=(\nu_1-\nu_3,\nu_1-\nu_2,0)$; the agent proves this via the Racah–Speiser form of Weyl's character formula for $\mathrm{GL}_3$ (the number of irreducible constituents of $V_\lambda\otimes W$ equals that of $V_\lambda\otimes W^*$), reduced to a sign identity $\sum_{\sigma\in S_3}\mathrm{sgn}(a+\sigma b)=\sum_\sigma\mathrm{sgn}(a-\sigma b)$ that is piecewise constant on the cells of a hyperplane arrangement and is settled by a finite check. (My own direct computation for $k\le7$ shows agreement even on $d_0,\dots,d_{k+3}$; the exact disagreement window is $\{k+4,\dots,n-4\}$ for $k\le12$ by the agent's data.) Together with Proposition 4.6.1 and Proposition 4.10.5 this shows that bounded-length agreement at either end, or at both ends simultaneously with a linearly growing middle window, is compatible with non-conjugacy; **a counterexample would have to be controlled in the middle as well**.
-
-**Heuristic (CONJECTURAL, honest version).** Among the $26{,}613$ transpose classes at $n=42$ the conditional probability that two classes agreeing on $d_0,\dots,d_{a-1}$ also agree on $d_a$ *rises* with $a$ (to about $0.75$ at $a=15$): the bottom of the vector is "sticky", but every deeply agreeing pair found is structural (attachment or body-insertion pairs) and provably separates at the top. From the top, $P(\text{same }f)\approx1.4\cdot10^{-5}$, $P(\text{same }d_{n-3}\mid\text{same }f)\approx0.017$, $P(\text{same }d_{n-4}\mid\dots)\approx0.012$, and no pair with $n\le42$ agrees on the top six entries. If successive top levels behaved like independent polynomial invariants with conditional agreement probability $\le c<1$, the expected number of collisions at size $n$ would be at most $T_5(n)\,c^{n/2-6}$ with $T_5(n)$ (the number of depth-5 collisions, Section 4.10) growing only slowly — negligible for $n\ge60$ if $c\le0.1$. The only real input of this heuristic is genericity of the top levels relative to one another; every known mechanism forcing agreement (mirror box moves, body insertion, attachment) has bounded reach at one end and provably fails at the other. So a counterexample cannot be an accident of small numbers; it would need an identity controlling all $\approx n-4$ coordinates at once, as transposition does. The agent's verdict, which I share: unlikely but not excluded.
+with the two-row polynomials of Corollary 4.8. (A negative result from the same computation, VERIFIED for $n\le10$: there is no constant-coefficient bilinear differential identity expressing $\sum_cP_{\lambda-c}(t)P_{\lambda-c}(u)$ through $P_\lambda$ and its derivatives, so the multiset of children polynomials cannot be read off $P_\lambda$ by such a formula.)
+
+### 4.13 Attempts to disprove Conjecture B: independent confirmation, a both-ends family, and a heuristic
+
+A separate effort, whose proved claims were checked by two independent reviewers, tried to disprove Conjecture B.
+
+**Independent confirmation (COMPUTATIONALLY VERIFIED).** From-scratch $d$-vector code reproduces, for every $n\le42$, the counts, the number of transpose pairs, the absence of collisions and the digests of `data/collisions_python.txt`. A targeted search for pairs of transpose classes with the same multiset of corner boxes, the same $f^\lambda$ and the same $(C_2,C_1^2,C_4)$ finds none for $n\le52$.
+
+**Theorem 4.13.1 (a family agreeing at both ends; PROVED separately, independently re-verified for $k\le7$).** For $k\ge1$ let $\lambda_k=(5,3^k,2,2,2)$ and $\mu_k=(4,4,3^k,1,1,1)$, $n=3k+11$. Then $f^{\lambda_k}=f^{\mu_k}$, $C_2(\lambda_k)=C_2(\mu_k)$, $C_1(\lambda_k)-C_1(\mu_k)=2$, and $d_j(\lambda_k)=d_j(\mu_k)$ for $0\le j\le k$ and for $n-3\le j\le n$, while $d_{n-4}$ differs. So the two shapes agree on a prefix of length about $n/3$ *and* on the top four entries (the first place where $C_1^2$ enters, $d_{n-4}$, separates them). The bottom agreement rests on a "body insertion" lemma reducing it to $d_j((3^K,2,2,2))=d_j((3^K,1,1,1))$ for $j\le K$, which after transposition and a $180^\circ$ rotation becomes a symmetry $N_j(\nu)=N_j(\nu^*)$ of chain counts in the three-row part of Young's lattice, $\nu^*=(\nu_1-\nu_3,\nu_1-\nu_2,0)$; this is proved via the Racah–Speiser form of Weyl's character formula for $\mathrm{GL}_3$ (the number of irreducible constituents of $V_\lambda\otimes W$ equals that of $V_\lambda\otimes W^*$), reduced to a sign identity $\sum_{\sigma\in S_3}\mathrm{sgn}(a+\sigma b)=\sum_\sigma\mathrm{sgn}(a-\sigma b)$ that is piecewise constant on the cells of a hyperplane arrangement and is settled by a finite check. (Direct computation for $k\le7$ shows agreement even on $d_0,\dots,d_{k+3}$; the exact disagreement window is $\{k+4,\dots,n-4\}$ for $k\le12$ by the separate computation.) Together with Proposition 4.6.1 and Proposition 4.10.5 this shows that bounded-length agreement at either end, or at both ends simultaneously with a linearly growing middle window, is compatible with non-conjugacy; **a counterexample would have to be controlled in the middle as well**.
+
+**Heuristic (CONJECTURAL, honest version).** Among the $26{,}613$ transpose classes at $n=42$ the conditional probability that two classes agreeing on $d_0,\dots,d_{a-1}$ also agree on $d_a$ *rises* with $a$ (to about $0.75$ at $a=15$): the bottom of the vector is "sticky", but every deeply agreeing pair found is structural (attachment or body-insertion pairs) and provably separates at the top. From the top, $P(\text{same }f)\approx1.4\cdot10^{-5}$, $P(\text{same }d_{n-3}\mid\text{same }f)\approx0.017$, $P(\text{same }d_{n-4}\mid\dots)\approx0.012$, and no pair with $n\le42$ agrees on the top six entries. If successive top levels behaved like independent polynomial invariants with conditional agreement probability $\le c<1$, the expected number of collisions at size $n$ would be at most $T_5(n)\,c^{n/2-6}$ with $T_5(n)$ (the number of depth-5 collisions, Section 4.10) growing only slowly — negligible for $n\ge60$ if $c\le0.1$. The only real input of this heuristic is genericity of the top levels relative to one another; every known mechanism forcing agreement (mirror box moves, body insertion, attachment) has bounded reach at one end and provably fails at the other. So a counterexample cannot be an accident of small numbers; it would need an identity controlling all $\approx n-4$ coordinates at once, as transposition does. The verdict: unlikely but not excluded.
@@ -378 +378 @@
-**Setting.** A strict partition $\lambda=(\lambda_1>\dots>\lambda_\ell>0)$ has shifted diagram with row $i$ occupying the boxes $(i,i),\dots,(i,i+\lambda_i-1)$. Covers in the shifted Young lattice: a box can be added to row $i$ iff $i=1$ or $\lambda_{i-1}\ge\lambda_i+2$, and a new row of length $1$ can be started iff $\lambda_\ell\ge2$ (or $\lambda=\emptyset$); a box can be removed from row $i$ iff $i=\ell$ or $\lambda_i\ge\lambda_{i+1}+2$. The tree $\mathrm{SSYT}$ of shifted standard tableaux (= saturated chains from $\emptyset$), its census $s_k(\lambda)$ and down-census $d_j(\lambda)$ are defined as before; $g^\lambda$ is the number of shifted SYT of shape $\lambda$ (shifted hook formula $g^\lambda=\frac{n!}{\prod\lambda_i!}\prod_{i<j}\frac{\lambda_i-\lambda_j}{\lambda_i+\lambda_j}$). Worley's Problem 2 conjectures that $\mathrm{SSYT}$ has only the trivial automorphism and that each node is determined by its census (necessarily: among nodes of the same depth, since all tableaux of one shape have identical subtrees). The work in this section was carried out by a background agent (`src/shifted.py`, `src/shifted_scan.py`, `tests/test_shifted.py`, report `agent_notes/shifted.md`) and independently re-derived by a reviewing agent with its own box-set model of shifted diagrams (cover sets identical for all strict partitions of $n\le21$; all census data recomputed and identical for $n\le40$, $K=14$); the structural claims below were re-checked by hand.
+**Setting.** A strict partition $\lambda=(\lambda_1>\dots>\lambda_\ell>0)$ has shifted diagram with row $i$ occupying the boxes $(i,i),\dots,(i,i+\lambda_i-1)$. Covers in the shifted Young lattice: a box can be added to row $i$ iff $i=1$ or $\lambda_{i-1}\ge\lambda_i+2$, and a new row of length $1$ can be started iff $\lambda_\ell\ge2$ (or $\lambda=\emptyset$); a box can be removed from row $i$ iff $i=\ell$ or $\lambda_i\ge\lambda_{i+1}+2$. The tree $\mathrm{SSYT}$ of shifted standard tableaux (= saturated chains from $\emptyset$), its census $s_k(\lambda)$ and down-census $d_j(\lambda)$ are defined as before; $g^\lambda$ is the number of shifted SYT of shape $\lambda$ (shifted hook formula $g^\lambda=\frac{n!}{\prod\lambda_i!}\prod_{i<j}\frac{\lambda_i-\lambda_j}{\lambda_i+\lambda_j}$). Worley's Problem 2 conjectures that $\mathrm{SSYT}$ has only the trivial automorphism and that each node is determined by its census (necessarily: among nodes of the same depth, since all tableaux of one shape have identical subtrees). The computations in this section (`src/shifted.py`, `src/shifted_scan.py`, `tests/test_shifted.py`) were independently re-derived with a separately written box-set model of shifted diagrams (cover sets identical for all strict partitions of $n\le21$; all census data recomputed and identical for $n\le40$, $K=14$); the structural claims below were re-checked by hand.
@@ -404,0 +405,3 @@
+---
+
+
