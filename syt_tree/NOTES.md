# NOTES.md — running log (tree of standard Young tableaux, Worley Problems 2–3)

## Session start
- Environment: Python 3.11, sympy 1.14, g++ available, 4 CPUs, 15 GB RAM. No Sage (not needed).
- Literature sweep launched as a background workflow (4 search angles + synthesis). Result recorded below when it arrives.

## Phase 0 — code
- `src/young.py`: partitions, conjugate, corners, hook lengths, f^λ (hook formula and recursion, cross-checked), f^{μ/λ} by chain counting, explicit SYT, involution numbers.
- `src/census.py`: d-vectors (recursion) and independent computation via skew chain counts; brute-force s_k by chain enumeration and independent skew route; the identity check s_k = Σ_j C(k,j) I_{k-j} d_j.
- `tests/test_basic.py`: 10 tests, all pass (hand-checked values f^{(3,2)}=5, f^{(2,2)}=2, s_1((2,1))=3, all d-vectors of size 4, etc.).

## Phase 1 — identity E_λ(t) = exp(t+t²/2) P_λ(t)
- VERIFIED coefficientwise for all |λ| ≤ 10 and k ≤ 10 (brute-force s_k vs. Σ_j C(k,j) I_{k-j} d_j); extended run n≤14,k≤14 in `data/identity_check_n14_k14.txt`.
- Proof found (two routes): (a) the differential-poset route: ω∘U = ω∘(D+I) and DU−UD=I give A' = (1+t)A + A_{Dλ}, hence A = e^{t+t²/2}B with B' = B_{Dλ}, B(0)=1, so B = P_λ. Holds in any 1-differential poset (r-differential: e^{r(t+t²/2)}). (b) the symmetric-function route sketched in the task (Hall inner product, p_1-shift). Written up in results.md.

## Literature (bounded search, done in background; synthesis saved to data/literature_search.md)
- Worley Problems 2/3 NOT solved (as of the Aug-2026 author draft; no citing paper addresses them).
- Verbatim Problem 3: automorphism defined per NODE of symmetric shape, "transposes all nodes in the subtree rooted at the node" = our τ_T. So the parity obstruction below is a genuine correction to the conjecture as stated.
- The identity E_λ = exp(t+t²/2) P_λ is KNOWN: Stanley, "On the enumeration of skew Young tableaux", Adv. Appl. Math. 30 (2003), Thm 2.2 eq. (10) (exp. specialization of Macdonald I.5 Ex. 27(a) = skew Littlewood identity); also a corollary of Stanley, Differential posets (1988), Cor. 2.6(c). Our differential-poset proof is essentially Stanley's mechanism, written out for a fixed start vertex.
- Neighbouring problems: Problem 8 (k-row SYT tree, rigid) and Problem 9 (Young–Fibonacci tree; automorphisms generated by local swaps but "not every automorphism is a finite product of local swaps") solved July 2026 (De Caro / OpenAI Codex, Zenodo 21538867, 21538865).

## Phase 2 — computation (Conjecture B)
- Exact Python scan: all partitions of n ≤ 60, zero collisions (data/collisions_python.txt; per-level sha256 and mod-p digests).
- C++ mod (2^61−1) scan: n ≤ 75, zero collisions (data/scan_modp_p0_n75.txt); digests agree with Python for n ≤ 60 (all levels). Second prime 2^62−57 to 60 (data/scan_modp_p1.txt), run to 75 in progress.
- Since d(λ)=d(μ) exactly ⇒ d(λ)≡d(μ) mod p, "no collision mod p" is a rigorous negative: Conjecture B holds for all |λ| ≤ 75.
- Timing: Python 6 min to n=60; C++ a few minutes to 75 (memory ≈ 9 GB at n=75).

## Phase 3 — automorphisms: the literal conjecture is FALSE, corrected form proved from B''
- Parity homomorphisms E_{λ,p}: Aut_1 → Z/2 (λ symmetric, p an off-diagonal mirror pair of addable corners) take equal values on every τ_T for the two pairs of λ=(3,2,1) but different values on the single-pair transpose θ_{T,p}. Hence θ ∉ closure⟨τ⟩. Confirmed with sympy at depth 7: |⟨τ⟩_7| = 2^27, |⟨θ⟩_7| = 2^43, θ ∉ ⟨τ⟩_7.
- Theorem: closure⟨θ⟩ = Aut ⟺ B'' (siblings with isomorphic subtrees are mirror images). Unconditionally: every automorphism agrees on levels ≤ 75 with a finite product of θ's.

## Phase 4 — Conjecture B, partial results
- Local product theorem (j ≤ 2m), rectangles, C_2 recoverable via Jucys–Murphy, Pfaffian formula, explicit two-row formula d_j = Σ_b C(j,b) sgn(δ+1−j+2b)[...]. Details in results.md.
- Hard pairs: (2,2,1^{n−4}) vs (3,1^{n−3}) agree on d_0..d_{n−4}.
- (f, C_2) alone does not separate (collisions from n=14).
- Background workflow (5 angles + referees) launched on: class C = two-row/two-column/hooks; fat hooks; top-end statistics; structure; skeptic. First launch failed on a session limit; relaunched.
